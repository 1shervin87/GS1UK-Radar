services:
  radar:
    build: .
    container_name: gs1uk-regulatory-radar
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////app/data/radar.db
    volumes:
      - radar-data:/app/data

volumes:
  radar-data:
